---
type: task
id: T02.03.Core
story: [[Modify Position]]
layer: core
cycle: 2
tags: [task]
created: 2026-05-30
---

# Modify Position — Frontend Core

## Description

Expose a `modifyPosition(positionId, ticker, quantity, operationDate)` action in `ui-core` that calls `PUT /portfolio/positions/{positionId}`.

## Notes

- Include the `Authorization: Bearer {token}` header
- Surface 404 and 400 error messages from the API response to the caller
- Export the `ModifyPositionInput` input type from `src/index.ts`
- Coordinate the interface with Web and Mobile so both can work in parallel
- Depends on T02.02 Core
