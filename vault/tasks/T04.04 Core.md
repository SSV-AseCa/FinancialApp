---
type: task
id: T04.04.Core
story: [[View Transaction History]]
layer: core
cycle: 3
tags: [task]
created: 2026-05-30
---

# View Transaction History — Frontend Core

## Description

Expose a `fetchTransactionHistory()` action in `ui-core` that calls `GET /portfolio/transactions` and returns the list of transactions.

## Notes

- Include the `Authorization: Bearer {token}` header
- Export the `Transaction` type (type, company, quantity, date) from `src/index.ts` — reuse the type from T04.02 Core if already defined
- Coordinate the interface with Web and Mobile so both can work in parallel
- Depends on T04.02 Core
