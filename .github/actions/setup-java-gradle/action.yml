name: Setup Java and Gradle
description: Installs Java 21 (Temurin) and configures the Gradle build action with caching.

runs:
  using: composite
  steps:
    - uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: temurin
    - uses: gradle/actions/setup-gradle@v4
