spring:
  application:
    name: ssv

edgar:
  base-url: ${EDGAR_BASE_URL:https://data.sec.gov}
  user-agent: ${EDGAR_USER_AGENT:FinancialApp santinocolombo13@gmail.com}
  submissions-path: ${EDGAR_SUBMISSIONS_PATH:/submissions/CIK%s.json}
  rate-limit:
    max-requests: ${EDGAR_RATE_LIMIT_MAX_REQUESTS:10}
    window-millis: ${EDGAR_RATE_LIMIT_WINDOW_MILLIS:1000}