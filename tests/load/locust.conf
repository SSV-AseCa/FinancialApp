# Shared Locust defaults. Override any value on the CLI (e.g. --host, --users).
# Usage: locust --config=locust.conf ...
locustfile = locustfile.py
host = http://localhost:8080
# Headless + CSV are the norm for documented runs; the runbook shows full commands.
