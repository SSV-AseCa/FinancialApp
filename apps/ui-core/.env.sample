# ui-core is a shared component library consumed by apps/web and apps/mobile.
# It does not connect to any service directly and requires no runtime variables.
# Add variables here if build-time feature flags are introduced.
