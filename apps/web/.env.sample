# ── API ───────────────────────────────────────────────────────────────────────
# Base URL of the backend API reachable from the browser.
# Vite exposes variables prefixed with VITE_ to client-side code.
VITE_API_BASE_URL=http://localhost:8080

# ── Auth0 ─────────────────────────────────────────────────────────────────────
# Auth0 application configuration
VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_AUDIENCE=https://api.ssvfinancial.com
VITE_AUTH0_CLIENT_ID=your-client-id

# ── Cypress E2E Tests ─────────────────────────────────────────────────────────
# NOTE: Cypress credentials and configuration variables (like TEST_USER_EMAIL/TEST_USER_PASSWORD)
# should NOT be placed in this runtime .env file.
# Instead, place them in apps/web/cypress.env.json (which is gitignored by default)
# or inject them via CI secrets.
#
# Placeholder credentials (unused as Cypress injects a mock token directly into localStorage):
# TEST_USER_EMAIL=cypress-test@ssvfinancial.com
# TEST_USER_PASSWORD=your-secure-test-password
