services:

  web:
    image: ${WEB_IMAGE:-ssv-web:latest}
    ports:
      - "3000:80"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 80"]
      interval: 5s
      timeout: 3s
      retries: 10
