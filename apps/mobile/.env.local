# Overrides .env at build time (Vite). Set VITE_USE_MOCK_AUTH=true ONLY for the
# mock-auth Appium specs; keep it false to exercise the real Auth0 in-app
# (Custom Tabs) login flow. Safe to delete.
VITE_USE_MOCK_AUTH=true
VITE_ENABLE_E2E_AUTH_BOOTSTRAP=true
